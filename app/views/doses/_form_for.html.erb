<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all.order(:name) %>
  <%= f.button :submit , :value => "add ingredient", :class => "button_create" %>
<% end %>
